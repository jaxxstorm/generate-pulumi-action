/**
 * @module std
 */
import { write } from './write';
export function log(value, options) {
    if (value === undefined) {
        V8Worker2.print('undefined');
        return;
    }
    write(value, '', options);
}
